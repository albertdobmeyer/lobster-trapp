# EXAMPLE ONLY — not used at runtime.
#
# The Tauri app orchestrates components via their component.yml manifests.
# Each component owns its own container topology (e.g. openclaw-vault has its
# own docker-compose.yml with vault + proxy containers). This file exists as
# a reference for developers who want a quick "bring up everything" shortcut,
# but it is NOT the canonical orchestration path.
#
# Runtime security model (onion/concentric):
#   Lobster-TrApp (GUI + orchestration, outermost)
#     └── OpenClaw-Vault (hardened container = passive defense)
#           └── ClawHub-Forge (skill scanning = active defense)
#                 └── MoltBook-Pioneer (agent in the wild, innermost)

services:
  # openclaw-vault:
  #   build: ./components/openclaw-vault
  #   security_opt:
  #     - no-new-privileges:true
  #   read_only: true

  # clawhub-forge:
  #   build: ./components/clawhub-forge
  #   depends_on:
  #     - openclaw-vault

  # moltbook-pioneer:
  #   build: ./components/moltbook-pioneer
  #   depends_on:
  #     - openclaw-vault
  #   network_mode: "service:openclaw-vault"
